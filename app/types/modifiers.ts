export type DvctModifierGroup = {
  _id: string
  name: string
  max: number
  min: number
  multiMax: number
  multiply: number
  plu: string
  nameTranslations: any // replace 'any' with the actual type if known
}
